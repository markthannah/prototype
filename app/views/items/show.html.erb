<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-xs-12">
    <h4 class="sub-item-name-show">
      VIEW MORE ITEMS FROM STERLING SILVER, HOME FURNISHINGS, DÉCOR & MORE
    </h4>
    <h1 class="item-name-show">
      <%= @item.name %>
    </h1>
    <hr>
    <h5 class="sub-item-name-show">
      WEDNESDAY, OCTOBER 18TH 2017 @ 10:07PM EDT
    </h5>
 </div>

 <div class="row">
   <div class="col-xs-12">
     <div class="row">
       <div class="col-xs-6">
         <i class="fa fa-picture-o" style="font-size:35em;" aria-hidden="true"></i>
       </div>
       <div class="col-xs-6" style="margin-top:35px;">
         <div class="well wellbig">
           <p class="centered item-name">This section includes bid infrormation, shipping details, and related items.</p>
         </div>
       </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12">
      <strong class="item-description">ITEM DETAILS</strong>
    </div>
    <div class="col-xs-12" style="padding-top:15px; padding-bottom:15px;">
      <p class="item-description"> The item's description would continue to display here.</p>
    </div>

<!---RINGS--------------------------------------------------------------------------------------->

    <% if @item.category == "Rings" %>

    <% unless @item.ringcount == 1 %>
    <div class="col-xs-12">
      <strong class="item-description" style="text-transform:uppercase;"><%= @item.ring1name %></strong>
    </div>
    <% else %>
    <% end %>
    <% unless @item.ringdesignern.blank? %>
    <div class="col-xs-12">
      <strong class="item-description">Ring Designer:</strong>
      <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Designer is Important and should be displayed, if present."></span>
      <%= @item.ringdesignern %>
    </div>
    <% else %>
    <% end %>
    <% unless @item.ringmetaltypeagain.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Metal Type:</strong>
      <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This is Required, and will always display. This is a multiselect field, so they'll appear comma separated here."></span>
      <%= @item.ringmetaltypeagain.join(", ") %>
    </div>
    <% else %>
    <% end %>
    <% unless @item.ringothermetalagain.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Accent Metal Type:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Accent metals should always display here in a comma separated list, if present."></span>
        <%= @item.ringothermetalagain.join(", ") %>
      </div>
    <% end %>
    <% unless @item.ringsizen.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Ring Size:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Ring size is Required and needs to be stored and displayed out to the hundredths place, even if the final digit is a zero."></span>
        <%= number_with_precision(@item.ringsizen, precision: 2) %>
      </div>
    <% else %>
    <% end %>
    <% unless @item.ringwidth.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Ring Width:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Ring width is used infrequently, but when it is used, it should appear with decimals out to the hundredths place."></span>
        <%= number_with_precision(@item.ringwidth, precision: 2) %> mm
      </div>
    <% else %>
    <% end %>
    <% unless @item.ringhallmarksz.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Hallmarks:</strong>
      <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Required, and always display."></span>
      <%= @item.ringhallmarksz %>
    </div>
    <% else %>
    <% end %>
    <% unless @item.ringengravedmarks.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Personalizations:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This is an open text field that appears infrequently."></span>
        <%= @item.ringengravedmarks %>
      </div>
    <% else %>
    <% end %>
    <% unless @item.ringtotalweighttype2.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Total Weight:</strong>
      <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Important, and display if present. If the weight is .ozt, storage and the view has to take the decimal out to the thousandths place, even if the final digit is a 0.</br>If the weight is .dwt, storage and the view has to take the decimal out to the hundreths place, even if the final digit is a 0."></span>
      <% if @item.ringweighttypez == 'ozt'%>
        <%= number_with_precision(@item.ringtotalweighttype2, precision: 3) %>
        <%= @item.ringweighttypez %>
        <% else %>
        <%= number_with_precision(@item.ringtotalweighttypez, precision: 2) %>
        <%= @item.ringweighttypez %>
      <% end %>
    </div>
    <% else %>
    <% end %>
  </div>

  <!---STONE 1--------------------------------------------------------------------------------------->

  <% unless @item.ringcstonetype.blank? %>
  <div class="row">
    <div class="col-xs-12 new-heading">
      <strong class="item-description" style="text-transform:uppercase;"><%=@item.ringcstoneposition%>
        <% if (@item.ringcstonecountfinal == nil) %>
          STONE
         <% elsif (@item.ringcstonecountfinal == 1 && @item.ringcstonetype != 'Diamond')%>
          STONE
        <% elsif (@item.ringcstonecountfinal > 1 && @item.ringcstonetype == 'Diamond')%>
          DIAMONDS
        <% elsif (@item.ringcstonecountfinal == 1 && @item.ringcstonetype == 'Diamond')%>
          DIAMOND
        <% elsif (@item.ringcstonecountfinal > 1 && @item.ringcstonetype != 'Diamond')%>
          STONES
        <% end %>
      </strong>
      <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Notice how this label combines the stone's position and the word 'Stone', or 'Stones' if the count is greater than one. Also, notice how 'Diamond' or 'Diamonds' appears in this heading depending on the stone count and type. See below for how this also affects whether type is listed in the template."></span>
    </div>
    <% unless @item.ringcstonecountfinal.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Count:</strong>
      <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Count will always be Required and display on the Item View Page, keeping in mind that when this value is greater than 1, that all stones with this type and in this position (center, side, or accent) have the same qualities listed below. If any of these characteristics vary, the stone will be added in a separate template."></span>
      <%= @item.ringcstonecountfinal.round %>
    </div>
    <% else %>
    <% end %>
    <% if @item.ringcstonetype != "Diamond" %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Type:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Type will only display if the stone type is not Diamond. It's hidden if Diamond is the type, since it's put in the heading."></span>
        <%= @item.ringcstonetype.join(", ") %>
      </div>
    <% else %>
    <% end %>
    <% unless @item.ringcstonetreatment.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Treatment:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Important, not Required. Display it, if present"></span>
        <%= @item.ringcstonetreatment %>
      </div>
    <% else %>
    <% end %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Shape:</strong>
      <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This will be Required and always display."></span>
      <%= @item.ringcstoneshape.join(", ") %>
    </div>
    <% if @item.ringcstonetype == "Diamond" %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Color Grade:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This will typically appear as a range. It should also accept a single value."></span>
        <%= @item.ringcstonecolorgrade %>
        <% unless @item.ringcstonecolorgradeto.blank? %>
            - <%= @item.ringcstonecolorgradeto %>
          <% else %>
          <% end %>
      </div>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Clarity Grade:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Same as Color Grade."></span>
        <%= @item.ringcstoneclaritygrade %>
        <% unless @item.ringcstoneclaritygradeto.blank? %>
            - <%= @item.ringcstoneclaritygradeto %>
          <% else %>
          <% end %>
      </div>
      <% else %>

      <% unless @item.ringcstonehue.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Hue:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This is Important, not Required. Notice how the code appears first for quick access by Gemology in the actual form. In this example, I'm only displaying the characters that follow the first forward slash, which is more user friendly."></span>
        <%= @item.ringcstonehue.partition('/').last %>
      </div>
      <% else %>
      <% end %>
      <% unless @item.ringcstonetone.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Tone:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This is Important, not Required, and will follow the same logic as Hue and Saturation."></span>
        <%= @item.ringcstonetone.partition('/').last %>
      </div>
      <% else %>
      <% end %>
      <% unless @item.ringcstonesaturation.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Saturation:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This is Important, not Required. Same as Tone and Hue."></span>
        <%= @item.ringcstonesaturation.partition('/').last %>
      </div>
      <% else %>
      <% end %>

      <% end %>

      <% if @item.ringcstonedimensiontype == "Single" %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Dimensions:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This will always display if the ring includes a stone. If only a diameter is measured, just the first dimension will display. Alternatively, if a length and width are measured, two dimensions will appear. If a depth is measured, L x W x D will display. This will also accept a range and estimated values in the parenthesees. If one or more value is estimated '- (estimate)' appears at the end. Measurements should always display up to the hundreths place."></span>
        <%= @item.ringcstonedimensionlsingle %> mm
          <% unless @item.ringcstonedimensionwsingle.blank? %>
            x <%= @item.ringcstonedimensionwsingle%> mm
            <%end%>
          <% unless @item.ringcstonedimensiondsingle.blank? %>
            x <%= @item.ringcstonedimensiondsingle%> mm
          <%end%>
          <% unless @item.ringcstonedimensionest.blank? %>
          - (estimate)
          <% else %>
          <% end %>
        </div>
      <% elsif @item.ringcstonedimensiontype == "Range" %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Dimensions:</strong>
        <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This will always be Required and display if the ring includes a stone. If only a diameter is measured, just the first dimension will display. Alternatively, if a length and width are measured, two dimensions will appear. If a depth is measured, L x W x D will display. Measurements should always display up to the hundreths place."></span>
        <%= @item.ringcstonedimensionl %> mm
          <% unless @item.ringcstonedimensionw.blank? %>
            x <%= @item.ringcstonedimensionw%> mm
            <%end%>
          <% unless @item.ringcstonedepth.blank? %>
            x <%= @item.ringcstonedepth%> mm
          <%end%>
          to
        <%= @item.ringcstonedimensionlend %> mm
          <% unless @item.ringcstonedimensionwend.blank? %>
            x <%= @item.ringcstonedimensionwend%> mm
            <%end%>
          <% unless @item.ringcstonedimensiondend.blank? %>
            x <%= @item.ringcstonedimensiondend %> mm
          <%end%>
          <% unless @item.ringcstonedimensionest.blank? %>
          - (estimate)
          <% else %>
          <% end %>
        </div>
      <% else %>
      <% end %>


      <% unless @item.ringcstonecaratweight.blank? %>
        <div class="col-xs-12 item-details">
          <strong class="item-description">Carat Weight:</strong>
          <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This is Important for everything besides Diamonds, which it's required for. It should always display if the value is present. Additionally, notice how 'ct' appears if the template includes a count of 1 stone. It changes to 'ctw' if it's 2 or more."></span>
          <%= number_with_precision(@item.ringcstonecaratweight, precision: 2) %>
          <% if @item.ringcstonecountfinal > 1 %>
            ctw
          <% else %>
            ct
          <% end %>
        </div>
        <% unless @item.ringcstonegemnotes.blank? %>
          <div class="col-xs-12 item-details">
            <strong class="item-description">Gemologist Note:</strong>
            <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="This is an open text field that appears infrequently in case the gemologist needs to point out something specific."></span>
            <%= @item.ringcstonegemnotes %>
          </div>
        <% else %>
        <% end %>
      <% else %>
      <% end %>
      </div>
      <% else %>
      <% end %>

      <% unless @item.ringcostumemetal.blank? %>
        <div class="col-xs-12 item-details">
          <strong class="item-description"> Metal Type:</strong>
          <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Normally a basic metal."></span>
          <%= @item.ringcostumemetal %>
        </div>
      <% end %>
      <% unless @item.ringcostumecount.blank? %>
        <div class="col-xs-12 item-details">
          <strong class="item-description">Number of Rings:</strong>
          <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="Number of rings in the lot."></span>
          <%= @item.ringcostumecount %>
        </div>
      <% end %>

<!---STONE 2--------------------------------------------------------------------------------------->
<% unless @item.ringsstonetype.blank? %>
<div class="row">
  <div class="col-xs-12 new-heading">
    <strong class="item-description" style="text-transform:uppercase;"><%=@item.ringsstoneposition%>
      <% if (@item.ringsstonecountfinal == nil) %>
        STONE
       <% elsif (@item.ringsstonecountfinal == 1 && @item.ringsstonetype != 'Diamond')%>
        STONE
      <% elsif (@item.ringsstonecountfinal > 1 && @item.ringsstonetype == 'Diamond')%>
        DIAMONDS
      <% elsif (@item.ringsstonecountfinal == 1 && @item.ringsstonetype == 'Diamond')%>
        DIAMOND
      <% elsif (@item.ringsstonecountfinal > 1 && @item.ringcstonetype != 'Diamond')%>
        STONES
      <% end %>
    </strong>
  </div>
  <% unless @item.ringsstonecountfinal.blank? %>
  <div class="col-xs-12 item-details">
    <strong class="item-description">Count:</strong>
    <%= @item.ringsstonecountfinal.round %>
  </div>
  <% else %>
  <% end %>
  <% if @item.ringsstonetype != "Diamond" %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Type:</strong>
      <%= @item.ringsstonetype.join(", ") %>
    </div>
  <% else %>
  <% end %>
  <% unless @item.ringsstonetreatment.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Treatment:</strong>
      <%= @item.ringsstonetreatment %>
    </div>
  <% else %>
  <% end %>
  <div class="col-xs-12 item-details">
    <strong class="item-description">Shape:</strong>
    <%= @item.ringsstoneshape.join(", ") %>
  </div>
  <% if @item.ringsstonetype == "Diamond" %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Color Grade:</strong>
      <%= @item.ringsstonecolorgrade %>
      <% unless @item.ringsstonecolorgradeto.blank? %>
          - <%= @item.ringsstonecolorgradeto %>
        <% else %>
        <% end %>
    </div>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Clarity Grade:</strong>
      <%= @item.ringsstoneclaritygrade %>
      <% unless @item.ringsstoneclaritygradeto.blank? %>
          - <%= @item.ringsstoneclaritygradeto %>
        <% else %>
        <% end %>
    </div>
    <% else %>

    <% unless @item.ringsstonehue.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Hue:</strong>
      <%= @item.ringsstonehue.partition('/').last %>
    </div>
    <% else %>
    <% end %>
    <% unless @item.ringsstonetone.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Tone:</strong>
      <%= @item.ringsstonetone.partition('/').last %>
    </div>
    <% else %>
    <% end %>
    <% unless @item.ringsstonesaturation.blank? %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Saturation:</strong>
      <%= @item.ringsstonesaturation.partition('/').last %>
    </div>
    <% else %>
    <% end %>

    <% end %>

    <% if @item.ringsstonedimensiontype == "Single" %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Dimensions:</strong>
      <%= @item.ringsstonedimensionlsingle %> mm
        <% unless @item.ringsstonedimensionwsingle.blank? %>
          x <%= @item.ringsstonedimensionwsingle%> mm
          <%end%>
        <% unless @item.ringsstonedimensiondsingle.blank? %>
          x <%= @item.ringsstonedimensiondsingle%> mm
        <%end%>
        <% unless @item.ringsstonedimensionest.blank? %>
        - (estimate)
        <% else %>
        <% end %>
      </div>
    <% elsif @item.ringsstonedimensiontype == "Range" %>
    <div class="col-xs-12 item-details">
      <strong class="item-description">Dimensions:</strong>
      <%= @item.ringsstonedimensionl %> mm
        <% unless @item.ringsstonedimensionw.blank? %>
          x <%= @item.ringsstonedimensionw%> mm
          <%end%>
        <% unless @item.ringsstonedepth.blank? %>
          x <%= @item.ringsstonedepth%> mm
        <%end%>
        to
      <%= @item.ringsstonedimensionlend %> mm
        <% unless @item.ringsstonedimensionwend.blank? %>
          x <%= @item.ringsstonedimensionwend%> mm
          <%end%>
        <% unless @item.ringsstonedimensiondend.blank? %>
          x <%= @item.ringsstonedimensiondend%> mm
        <%end%>
        <% unless @item.ringsstonedimensionest.blank? %>
        - (estimate)
        <% else %>
        <% end %>
      </div>
    <% else %>
    <% end %>


    <% unless @item.ringsstonecaratweight.blank? %>
      <div class="col-xs-12 item-details">
        <strong class="item-description">Carat Weight:</strong>
        <%= number_with_precision(@item.ringsstonecaratweight, precision: 2) %>
        <% if @item.ringsstonecountfinal > 1 %>
          ctw
        <% else %>
          ct
        <% end %>
      </div>
      <% unless @item.ringsstonegemnotes.blank? %>
        <div class="col-xs-12 item-details">
          <strong class="item-description">Gemologist Note:</strong>
          <%= @item.ringsstonegemnotes %>
        </div>
      <% else %>
      <% end %>
    <% else %>
    <% end %>
    </div>
    <% else %>
    <% end %>
    <% if @item.ringcount.to_i > 1 %>
    <div class="col-xs-6" style="padding-top:40px; padding-left:0px; padding-bottom:40px;">
      <strong class="item-description" style="text-transform:uppercase;"><%= @item.ring2name %></strong>
    <div>
        If the cataloger decided to reuse the first set of Ring Details, they appear here again, along with any new stones, following the format used above. If they did not chose to resuse the item details from Ring 1, the new set of details, stones, etc. will apper. Ring three, etc. will display following the same pattern below this ring.
      </div>
    </div>
    <% else %>
    <% end %>
<!---STONE DISCLAIMERS----------------------------------------------------------------------------->

  <% unless @item.ringgeodisclaimer.blank? %>
  <div class="row">
    <div class="col-xs-7">
      <p class="item-description">
        <ul>
          <li>
            All diamond and gemstone grading is done under GIA standards as the mounting permits, where gemstones are present. All weights and measurements are estimated and approximate. Industry standard and accepted treatments for gemstones are assumed unless otherwise noted.
            <span class="fa fa-info-circle" data-toggle="tooltip" data-html="true" title="No change to the logic for these disclaimers."></span>
          </li>
        </ul>
      </p>
    </div>
  </div>
  <% else %>
  <% end %>

  <% unless @item.ringgemdisclaimer.blank? %>
  <div class="row">
    <div class="col-xs-12">
      <p class="item-description">
        <ul>
          <li>
            This item has been evaluated and verified by an independent Graduate Gemologist. (<%=@item.ringcode%>).
        </li>
      </ul>
      </p>
    </div>
  </div>
  <% else %>
  <% end %>


  <% else %>
  <% end %>

</br></br></br></br></br></br></br></br></br></br></br></br>

<script>
$(document).ready(function(){
  $('[data-toggle="tooltip"]').tooltip();
});
</script>
